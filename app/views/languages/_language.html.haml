.language-container{:class => "#{language.name}"}
  .row
    .heading
      .container
        = link_to language.name, language_path(language)
  .container
    %ul.col-md-12
      - if params[:action] == "index"
        = render language.links.first(3)
      - else
        = render language.links
